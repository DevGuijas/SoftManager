<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>SoftManager</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <aside>
        <h2>SoftManager</h2>
        
        <div style="padding: 0 15px; font-size: 12px; color: #bdc3c7; margin-bottom: 20px;">
            Olá, <strong><%= typeof user !== 'undefined' ? user.nome : 'Visitante' %></strong><br>
            <span style="color: #7f8c8d"><%= typeof user !== 'undefined' ? user.cargo : '' %></span>
        </div>

        <nav>
            <a href="/">📊 Dashboard</a>
            <a href="/clientes">👥 Clientes</a>
            <a href="/projetos">🚀 Projetos</a>
            <a href="/logout" style="color: #e74c3c; border-top: 1px solid #3d5266; margin-top: 20px;">🚪 Sair</a>
        </nav>
    </aside>
    <main>